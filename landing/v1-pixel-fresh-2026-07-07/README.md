# 落地页 v1 · 像素清新版（存档）

> 存档时间：2026-07-07
> 风格：清新暖白底 + 像素风（2px 硬边 + 硬偏移阴影 + 方角）+ 四个鲜艳点缀色（天蓝/薄荷绿/珊瑚红/柠檬黄）+ 亮暗双主题。
> 亮点：Hero 里一个 Canvas 画的会动像素猫演示（思考→冒「✅任务完成」气泡→待机循环）。

这是用户看过、说"也不错"的第一版。**保险备份**，之后按新方向（参考 Framer 广告那种「上部纯白留白 + 底部一排彩色圆角图标」）迭代时，若想回退到这版，直接用本目录的 index.html 覆盖回 ../index.html 即可。

内容结构：顶栏 → Hero(像素猫演示) → 监控(三卡片+覆盖矩阵) → 换肤 → 隐私 → 下载CTA → 页脚。
自包含单文件，双击浏览器即可打开。
